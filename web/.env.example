# 复制为 web/.env.local 并填入。前端只读链上数据，不需要钱包/私钥。
# 留空 REGISTRY_ADDRESS 则进入 DEMO 模式（用本地 mock 数据演示，核验照样可用）。

VITE_REGISTRY_ADDRESS=        # 部署后回填的 LPAgentRegistry 地址（0x...）
VITE_RPC_URL=                 # Mantle Sepolia RPC，见 https://chainlist.org/chain/5003
VITE_CHAIN_ID=5003
